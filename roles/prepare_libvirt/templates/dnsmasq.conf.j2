bind-interfaces
listen-address={{ ansible_default_ipv4.address }}
local=/{{ net.domain }}/
address=/apps.{{ net.subdomain }}.{{ net.domain }}/{{ ansible_default_ipv4.address }}
{% for c in groups['control'] %}
srv-host=_etcd-server-ssl._tcp.{{ net.subdomain }}.{{ net.domain }},{{ c }}.{{ net.subdomain }}.{{ net.domain }},2380,0,10
{% endfor %}
addn-hosts=/etc/hosts.openshift
